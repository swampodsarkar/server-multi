services:
  - type: web
    name: watch-time-tester
    runtime: node
    buildCommand: bash setup.sh
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    plan: free